{
  "name": "micro",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "local:build": "sam build --use-container",
    "aws:package": "sam package --region eu-central-1 --s3-bucket peng-data --output-template-file output-template.yaml",
    "aws:deploy": "sam deploy --template-file output-template.yaml",
    "install:book": "cd book && npm install",
    "install:bookCover": "cd book-cover && npm install",
    "install:booktrailer": "cd booktrailer && npm install",
    "postinstall": "npm run install:book && npm run install:bookCover && npm run install:booktrailer"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
